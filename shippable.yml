# Build Environment

language: python

python:
  - 3.0

install: true

# the build scripts
script:
- curl -LO https://raw.githubusercontent.com/clearfraction/tools/master/build.sh && chmod +x build.sh && docker run --privileged=true --cap-add=SYS_ADMIN --security-opt seccomp:unconfined -v $(pwd):/home  clearlinux:latest /bin/sh -c "cd /home && ./build.sh"   


notifications:
slack: unitedrpms:lzDykHTpajKrgmGz9NkrO1Hj





