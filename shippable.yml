# Build Environment

language: python

python:
  - 3.0

install: true

# the build scripts
script:
-  cd /home && curl -LO https://raw.githubusercontent.com/clearfraction/tools/master/build.sh && chmod +x build.sh && docker run --privileged=true --cap-add=SYS_ADMIN --security-opt seccomp:unconfined -v $(pwd):/home  clearlinux:latest /bin/sh -c "./build.sh"   


notifications:
slack: unitedrpms:lzDykHTpajKrgmGz9NkrO1Hj





